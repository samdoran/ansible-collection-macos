- name: Install Python on macOS
  hosts: "{{ macos_hosts | default('all') }}"
  gather_facts: no
  become: yes

  tasks:
    - import_role:
        name: samdoran.macos.command_line_tools
        tasks_from: bootstrap.yml
